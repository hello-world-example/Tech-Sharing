<?xml version="1.0" encoding="utf-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd"
       default-autowire="no"
       default-lazy-init="true">


    <!-- JadeBeanFactoryPostProcessorPostProcessor 将扫描符合规范的DAO接口，并作为spring容器的bean -->

    <!-- 如果要禁用 JadeBeanFactoryPostProcessorPostProcessor，

        请设置系统属性 jade.context.spring 为 0 (0代表disable, 1代表enable)。

        也可以通过设置 jade.context.spring.包名或类名单独为各个 package 或 类做定制(也是设置disable或enable），
        如果给定的类或 package 没有设置，则逐级使用父级的设置，
        jade.context.spring.com.yourcompany.dao.UserDAO 的父级是 jade.context.spring.com.yourcompany.dao;
        jade.context.spring.com 的父级是 jade.context.spring.*; jade.context.spring.* 是根，没有父级别
        因此您可以将 jade.context.spring.* 的设置看成整个系统的默认值
        -->
    <bean class="net.paoding.rose.jade.context.spring.JadeBeanFactoryPostProcessor"/>

</beans>
